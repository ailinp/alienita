{% extends '../themes/'~ page.theme|default('default') ~'/base.twig' %}

{% block content %}
{% embed '../components/top-heading.twig' %}
    {% block link %}{{ topHeading.links[locale].toPhotography|raw }}{% endblock %}
    {% block claim %}{{ topHeading.branding[locale]|raw }}{% endblock %}
{% endembed %}

<section class="Branding">
    <div class="u-wrapper row">
        <div class="{{ page.commonStuff.class.mainSection|raw }}">
            <p class="homeMainClaim">{{ mainSection.home[locale]|raw }}</p>
        </div>
        {% embed '../components/aside.twig' %}
            {% block title %}{{ aside.introduction.forBranding.title.en|raw }}{% endblock %}
            {% block description %}{{ aside.introduction.forBranding.description.en|raw }}{% endblock %}
        {% endembed %}
    </div>
</section>

{% endblock %}