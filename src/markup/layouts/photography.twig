{% extends '../themes/'~ page.theme|default('default') ~'/base.twig' %}

{% block content %}
{% embed '../components/top-heading.twig' %}
    {% block link %}{{ topHeading.links[locale].toPublication|raw }}{% endblock %}
    {% block claim %}{{ topHeading.photography[locale]|raw }}{% endblock %}
{% endembed %}

<section class="Photography">
    <div class="u-wrapper row">
        <div class="{{ page.commonStuff.class.mainSection|raw }}">
            <p class="homeMainClaim">{{ mainSection.home[locale]|raw }}</p>
        </div>
        {% embed '../components/aside.twig' %}
            {% block title %}{{ aside.introduction.forPhotography.title.en|raw }}{% endblock %}
            {% block description %}{{ aside.introduction.forPhotography.description.en|raw }}{% endblock %}
        {% endembed %}
    </div>
</section>

{% endblock %}